import EndureOS from '@/components/EndureOS'

// Standalone design preview — full EndureOS SPA with mock data
// No auth wall — accessible at /endure
export default function EndurePage() {
  return <EndureOS />
}
